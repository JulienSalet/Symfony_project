{% extends 'base.html.twig' %}

{% block main %}
    <div class="my-5">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header">
                            {{ product.name }}
                        </div>
                        <div class="card-body">
                            Prix : {{ product.price }}€
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}